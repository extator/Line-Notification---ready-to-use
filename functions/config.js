const lineCredential = {
    ACCESS_TOKEN: '*line massage api token*'
}
const firebaseCredential = {
    DATABASE_URL: '*firebase database url*'
}
module.exports = {
    lineCredential,
    firebaseCredential
}
